import React from 'react';
import { Typography } from 'antd';
const { Title } = Typography;

const CreateUser = () => {
  return (
    <div>
      <Title level={3} style={{ textAlign: 'center' }}>CREATE USER</Title>
    </div>
  );
};

export default CreateUser;